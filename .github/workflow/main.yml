# Fetches new data and generates charts.

name: Daily Update

on:
  push:
    branches:
      - master

jobs:
  daily_update:
    steps:
      - name: Update data and output
        run: python update.py
      - name: Commit and merge changes
        run: sh 'git status'
        
